# Vercel server variables. Never expose these with a VITE_ prefix.
GITHUB_TOKEN=github_fine_grained_token
GITHUB_DATA_REPO=owner/private-data-repository
# Optional when the frontend calls a different API origin.
SYNC_ALLOWED_ORIGIN=https://your-todotime-domain.example
