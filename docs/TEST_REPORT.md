# TodoTime 测试报告

测试日期：2026-08-14  
测试对象：个人识别码隔离、任务管理、计时器、手动补录、统计图表、并发同步、主题系统与数据保护

## 自动化检查

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| 逻辑测试 | `npm run test:logic` | 通过（23/23） |
| ESLint | `npm run lint` | 通过 |
| TypeScript + 前端生产构建 | `npm run build` | 通过 |
| 服务端 API 类型检查 | `npm run typecheck:api` | 通过 |
| 完整检查 | `npm test` | 通过 |
| 补丁格式 | `git diff --check` | 通过 |

逻辑测试覆盖 13 个主题及二级选择弹窗、计时阈值、圆环端点位置与边界、手动补录默认归属与类别继承、同步身份隔离、报告语料，以及多设备番茄事件合并和重复事件去重。

## 浏览器交互回归

| 场景 | 预期结果 | 结果 |
| --- | --- | --- |
| 本地应用启动 | 显示计时器和任务清单，无运行错误 | 通过 |
| 开始计时 | 按钮切换为“暂停”，剩余时间真实递减 | 通过 |
| 时长与番茄阈值 | 满 1 分钟保存时长，满 15 分钟才计番茄；旧记录按时长兼容 | 通过 |
| 跳过工作阶段 | 推进组次；满 1 分钟保存时长，满 15 分钟增加番茄 | 通过 |
| 暂停计时 | 倒计时停止，可继续 | 通过 |
| 创建任务 | 新任务进入列表，任务计数同步更新 | 通过 |
| 创建子任务 | 子任务保存并显示独立状态和开始按钮 | 通过 |
| 子任务快速开始 | 计时器显示子任务名称并开始倒计时 | 通过 |
| 手动补录到已有任务 | 时长、类别和任务归属正确，达到阈值后任务增加 1 个番茄 | 通过 |
| 手动补录并新建任务 | 新任务进入清单，补录记录立即关联并同步 | 通过 |
| 修改工作时长 | 暂停状态下立即更新计时器总时长 | 通过 |
| 任务完成/恢复 | 完成计数正确增加，取消完成后恢复 | 通过 |
| 子任务完成/恢复 | 子任务状态可以双向切换 | 通过 |
| 打开统计页 | 近七天趋势和板块占比正常渲染 | 通过 |
| 综合趋势图 | 同图显示时长、番茄、任务，支持近七天/近一个月切换 | 通过 |
| 混合图类型 | 专注时长为柱状图，番茄和任务为两条折线 | 通过 |
| 饼图分布 | 可切换时长、番茄、任务三类板块占比 | 通过 |
| 识别码默认状态 | 输入框隐藏内容，未生成时不能启用新身份 | 通过 |
| 生成识别码 | 生成合法 `TT-xxxxx-xxxxx-xxxxx-xxxxx` 格式并允许启用 | 通过 |
| 显示/隐藏识别码 | 输入框在文本和密码显示之间正确切换 | 通过 |
| 清除数据确认 | 明确提示只清当前身份本机缓存，不删除云端和其他身份 | 通过 |
| 页面错误日志 | 关键路径无 React 或运行时错误 | 通过 |
| 390×844 移动端视口 | 底部导航、任务内容和主要操作均正常渲染 | 通过 |
| 320×568 / 768×1024 / 1440×900 | 底栏贴底、宽度随内容区适配，无横向溢出 | 通过 |
| 生产构建页面 | 390×844 渲染正常，控制台无运行错误 | 通过 |

## 代码路径核查

| 规则 | 核查结果 |
| --- | --- |
| 不同识别码使用不同本地存储键 | 通过 |
| 服务端只使用识别码哈希目录 | 通过 |
| 原始识别码不写入 GitHub 数据文件 | 通过 |
| 配置和历史同步采用串行队列 | 通过 |
| 配置、历史分别防抖 | 通过 |
| 无变化记录不重复上传 | 通过 |
| GitHub 409/422 冲突可触发重试 | 通过 |
| 主任务、子任务均可作为计时归属 | 通过 |
| 未分配记录不会被后来任务自动认领 | 通过 |
| 今日与云端记录按 ID 去重 | 通过 |
| 本地与云端历史并集合并，不因空数据丢失记录 | 通过 |
| 两台设备同时新增的专注事件按唯一 ID 取并集 | 通过 |
| 同一专注事件重复同步不重复增加任务番茄 | 通过 |
| 任务番茄计数与普通字段修改分别合并 | 通过 |
| 旧版未分区数据按身份恢复 | 通过 |
| 页面可见时每分钟执行完整双向同步 | 通过 |
| 分钟检查点使用同一记录 ID 覆盖更新，不产生重复数据 | 通过 |
| 统计日期使用本地时区 | 通过 |
| 清除操作不使用 `localStorage.clear()` | 通过 |

## 尚未自动化的验证

- 两台真实设备同时离线编辑同一任务后再联网的人工冲突演练。
- 浏览器休眠数小时后的真实后台计时恢复测试。
- GitHub API 限流、Vercel 故障和网络长期中断的故障注入测试。
- 超过 400 天历史数据的实际裁剪测试。

这些场景已有代码级保护，但如要作为长期产品运行，建议后续加入可重复的集成测试和故障注入环境。
